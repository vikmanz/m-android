<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav_graph"
    app:startDestination="@id/myProfileFragment">

    <fragment
        android:id="@+id/myProfileFragment"
        android:name="com.vikmanz.shpppro.ui.main.my_profile.MyProfileFragment"
        android:label="fragment_my_profile"
        tools:layout="@layout/fragment_my_profile">
        <action
            android:id="@+id/startMyContacts"
            app:destination="@id/myContactsFragment" />
        <argument
            android:name="email"
            app:argType="string"
            android:defaultValue="login.fromnavigator@gmail.com"/>
    </fragment>


    <fragment
        android:id="@+id/myContactsFragment"
        android:name="com.vikmanz.shpppro.ui.main.my_contacts_list.MyContactsListFragment"
        android:label="fragment_my_contacts"
        tools:layout="@layout/fragment_my_contacts_list">
        <action
            android:id="@+id/startContactDetails"
            app:destination="@id/contactDetailsFragment">
        </action>
    </fragment>


    <fragment
        android:id="@+id/contactDetailsFragment"
        android:name="com.vikmanz.shpppro.ui.main.contact_details.ContactDetailsFragment"
        android:label="fragment_contact_details"
        tools:layout="@layout/fragment_contact_details">
        <argument
            android:name="contactID"
            app:argType="long" />
    </fragment>

</navigation>